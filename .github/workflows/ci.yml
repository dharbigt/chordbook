name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-and-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile check
        run: |
          python -m compileall app wsgi.py

      - name: Flask import smoke test
        env:
          FLASK_SECRET_KEY: ci-smoke-secret
        run: |
          python -c "from wsgi import app; print(app.name)"

      - name: Unit tests
        env:
          FLASK_SECRET_KEY: ci-test-secret
        run: |
          python -m unittest discover -s tests -p "test_*.py"
